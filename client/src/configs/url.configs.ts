export const PUBLIC_URL = {
  signIn: '/',
};

export const API_AUTH_URL = {
  signIn: '/auth/login',
  signUp: '/auth/register',
  tokens: '/auth/token',
  authMe: '/auth/me',
};

export const ComicsPathConfig = {
  origin: '/comics',
};
